import { ShellPlaceholder } from "@/components/shell-placeholder";

export default function SuperAdminPage() {
  return (
    <ShellPlaceholder
      title="Super Admin"
      surface="super"
      description="Gestión al 100% de Organizations, planes y módulos activables en toda la plataforma."
      pendingNote="PENDIENTE de producto. Ver docs/ALCANCES.md §5. El prototipo oro no lo incluye."
    />
  );
}
